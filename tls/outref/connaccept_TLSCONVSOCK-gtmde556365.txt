# *****************************************************************
# GTM-DE556365 - Test the following release note
# *****************************************************************
#
# Release note (http://tinco.pair.com/bhaskar/gtm/doc/articles/GTM_V7.1-002_Release_Notes.html#GTM-DE556365) says:
#
# The Receiver Server, configured to use TLS, continues waiting for new connections when a Source Server fails to establish a TLS session (e.g. misconfiguration). Previously, a Receiver Server configured for TLS but without -PLAINTEXTFALLBACK would exit with TLSCONVSOCK error message when it failed to establish a TLS session. (GTM-DE556365)

### Test that a replication receiving server issues a TLSCONVSOCK error and terminates when
### the source server terminates due to a TLS configuration error.
### For more details, see the thread at https://gitlab.com/YottaDB/DB/YDBTest/-/issues/687#note_3109611733
# Create a database
# Revise INSTANCE2.key to point to non-existent key to force Receiver Server to fail to establish TLS connection
==Executing MULTISITE_REPLIC 'RUN INST2 sed -i "s/INSTANCE2.key/INSTANCE456.key/" gtmcrypt.cfg'==
# Start the source instance and receiving instance
# Wait for receiver server log file to show the TLSCONVSOCK error message
# Confirm the receiver server terminates after issuing a TLSCONVSOCK error
# Stop the source instance
# Expect no 'Waiting for a connection' message after TLSCONVSOCK in the receiver server log. Previously, at least one such message would appear.
PASS: Receiver server stopped accepting connections after TLSCONVSOCK
# Confirm only one TLSCONVSOCK error was generated.
# Previously, the receiver server would continue waiting for connections and generate multiple TLSCONVSOCK errors.
----------
##TEST_AWKError YDB-E-TLSCONVSOCK seen in ##REMOTE_TEST_PATH##/instance2/##FILTERED##RCVR_##TIMESTAMP##.log as expected:
%YDB-E-TLSCONVSOCK, Failed to convert Unix TCP/IP socket to TLS/SSL aware socket
----------
# Confirm only one 'YDB-I-TEXT, Private Key corresponding to TLSID' message was generated.
# Previously, the receiver server would continue waiting for connections and generate multiple YDB-I-TEXT errors of this sort.
----------
Error YDB-I-TEXT seen in ##REMOTE_TEST_PATH##/instance2/##FILTERED##RCVR_##TIMESTAMP##.log as expected:
%YDB-I-TEXT, Private Key corresponding to TLSID: INSTANCE2 - error opening file ##TEST_COM_PATH##/tls/certs/INSTANCE456.key: No such file or directory.
----------
