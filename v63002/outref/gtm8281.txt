# Generating big string with 32756 characters
# temp.m contains big string

# Using string as source line in an m file
# a <repeated 32756 times> executed correctly

# Using string as an xecute string
# a <repeated 32756 times> executed correctly

# Using string as a command in direct mode
# a <repeated 32756 times> executed correctly

# --------------------------------------------------------------------

# Generating big string with 32757 characters
# temp.m contains big string

# Using string as source line in an m file
# a <repeated 32757 times> executed correctly

# Using string as an xecute string
# a <repeated 32757 times> executed correctly

# Using string as a command in direct mode
# a <repeated 32757 times> executed correctly

# --------------------------------------------------------------------

# Generating big string with 32758 characters
# temp.m contains big string

# Using string as source line in an m file
# a <repeated 32758 times> too long
%YDB-W-LSINSERTED, Line 1, source module ##TEST_PATH##/temp32758.m exceeds maximum source line length; line seperator inserted, terminating scope of any prior IF, ELSE, or FOR
Source lines exceeding 1023 character width are not displayed
		At column 32767, line 1, source module ##TEST_PATH##/temp32758.m
%YDB-E-SPOREOL, Either a space or an end-of-line was expected but not found
	"
	^-----
		At column 0, line 2, source module ##TEST_PATH##/temp32758.m
%YDB-E-LSEXPECTED, A line separator is expected here
%YDB-E-SPOREOL, Either a space or an end-of-line was expected but not found
		At M source location +1^temp32758

YDB>

# Using string as an xecute string
# a <repeated 32758 times> executed correctly

# Using string as a command in direct mode
# a <repeated 32758 times> executed correctly

# --------------------------------------------------------------------

# Generating big string with 32759 characters
# temp.m contains big string

# Using string as source line in an m file
# a <repeated 32759 times> too long
%YDB-W-LSINSERTED, Line 1, source module ##TEST_PATH##/temp32759.m exceeds maximum source line length; line seperator inserted, terminating scope of any prior IF, ELSE, or FOR
Source lines exceeding 1023 character width are not displayed
		At column 32767, line 1, source module ##TEST_PATH##/temp32759.m
%YDB-E-SPOREOL, Either a space or an end-of-line was expected but not found
	"
	^-----
		At column 0, line 3, source module ##TEST_PATH##/temp32759.m
%YDB-E-LSEXPECTED, A line separator is expected here
%YDB-E-SPOREOL, Either a space or an end-of-line was expected but not found
		At M source location +1^temp32759

YDB>

# Using string as an xecute string
# a <repeated 32759 times> too long
%YDB-E-INDRMAXLEN, Maximum length 32766 of an indirection argument was exceeded
		At M source location xecutefn+1^gtm8281

YDB>

# Using string as a command in direct mode
# a <repeated 32759 times> too long
%YDB-E-INDRMAXLEN, Maximum length 32766 of an indirection argument was exceeded

# --------------------------------------------------------------------

# Generating big string with 32760 characters
# temp.m contains big string

# Using string as source line in an m file
# a <repeated 32760 times> too long
%YDB-W-LSINSERTED, Line 1, source module ##TEST_PATH##/temp32760.m exceeds maximum source line length; line seperator inserted, terminating scope of any prior IF, ELSE, or FOR
Source lines exceeding 1023 character width are not displayed
		At column 32767, line 1, source module ##TEST_PATH##/temp32760.m
%YDB-E-SPOREOL, Either a space or an end-of-line was expected but not found
	a"
	^-----
		At column 0, line 3, source module ##TEST_PATH##/temp32760.m
%YDB-E-LSEXPECTED, A line separator is expected here
%YDB-E-SPOREOL, Either a space or an end-of-line was expected but not found
		At M source location +1^temp32760

YDB>

# Using string as an xecute string
# a <repeated 32760 times> too long
%YDB-E-INDRMAXLEN, Maximum length 32766 of an indirection argument was exceeded
		At M source location xecutefn+1^gtm8281

YDB>

# Using string as a command in direct mode
# a <repeated 32760 times> too long
%YDB-E-INDRMAXLEN, Maximum length 32766 of an indirection argument was exceeded

# --------------------------------------------------------------------

