# This tests that ydbinstall successfully installs plugins for a 
# random set of plugs-in (--octo --gui --aim --encplugin --posix --zlib),
# and randomly picking --plugins-only and --utf8.
setenv gtm_chset UTF-8
unset echo
# Test that 8 of 8 tests succeeded
# Test succeeded.
