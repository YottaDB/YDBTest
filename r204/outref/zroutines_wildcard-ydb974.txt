# Test to ensure that $ZROUTINES allows glob patterns in file_names
# We want to run a compiled program that uses $ZROUTINES
# by setting it to a pattern with glob patterns that matches shared library files.
# This test assumes that gt_ld_shl_suffix is .so, its actual value is currently ##TEST_SHL_SUFFIX##

# So, we will now create 2 shared library files, ydb974la.so and ydb974lb.so
# Also a 3rd one with a very long name for ydb974h.m
# And a 4th one with an * in the filename for ydb974p.m
# we will also check that non-shared library files are not picked up, so we create one to check with.
# We create a file named ydb974la.so.golf to ensure that it will not be included, as that would be incorrect.
# for the patterns in the tests to pick up that filename.

# creating a directory that we do not have read permission on to test 
# a permission denied error when running ydb974m.m
# Also creating a directory with too many wildcard matches to test that error.

# First, try to get a single match to ydb974la.so with pattern *974la*.so
# Running routine from library a.
activated shared library a

# Try to match 2 files, ydb974la.so and ydb974lb.so, with pattern *974l*.so
# Running routine from library a
activated shared library a
# Running routine from library b
activated shared library b
# Now printing out the order of $zroutines to make sure that
# all the shared libraries come before the $gtm_dist as that is
# required. The shared libraries that match the same glob pattern may
# appear in any order
##TEST_AWKydb974l[ab]##TEST_SHL_SUFFIX## ydb974l[ab]##TEST_SHL_SUFFIX## ##SOURCE_REALPATH##\(##SOURCE_REALPATH##\)
# Testing to ensure that we cannot run shared library d.
# We should not be able to run this as the filename is
# ydb974la.so.golf, so it should not match our pattern.
%YDB-E-ZLINKFILE, Error while zlinking "ydb974ld",%YDB-E-FILENOTFND, File ydb974ld.m not found,%YDB-E-FILENOTFND, File ydb974ld.o not found
		At M source location START+15^ydb974b

YDB>

# Try to match ydb974la.so and ydb974lb.so using the full path with pattern path/*974l*.so
# Running routine from library a
activated shared library a
# Running routine from library b
activated shared library b

# Check that no error happens if no files match the glob pattern

# Check that zroutines will not match a file that is not a shared library file.
# We should have already created fakeLibraryFile.so for this. We will check fake*LibraryFile.so
# If zroutines does try to match the fake library file, that should cause an error, so if no error follows, all is well.

# ydb974f.m starting. This tests setting zroutines to a filename that is too long.
# We expect this to produce an error.
%YDB-E-ZROSYNTAX, $ZROUTINES syntax error: *,%YDB-E-FILEPATHTOOLONG, Filename including the path cannot be longer than 255 characters
##TEST_AWK		At M source location START\+[0-9]\^ydb974f

YDB>

# ydb974g.m starting. This tests setting zroutines to glob pattern that does not end in .so.
# We expect this to produce an error.
%YDB-E-ZROSYNTAX, $ZROUTINES syntax error: not*So##TEST_SHL_SUFFIX##.gulp,%YDB-I-TEXT, glob filename pattern must end in ##TEST_SHL_SUFFIX##
##TEST_AWK		At M source location START\+[0-9]+\^ydb974g

YDB>

# ydb974h.m starting. This tests with filename too long.
%YDB-E-ZROSYNTAX, $ZROUTINES syntax error: bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb.so,%YDB-E-FILEPATHTOOLONG, Filename including the path cannot be longer than 255 characters,%YDB-I-TEXT, A glob pattern matched a filename longer than 255 characters.
		At M source location START+2^ydb974h

YDB>

# ydb974i.m starting. This tests setting zroutines to a glob pattern that does not end in .so.
# In this case, the filename ends in .o.
# We expect this to produce an error.
%YDB-E-ZROSYNTAX, $ZROUTINES syntax error: not*So##TEST_SHL_SUFFIX##.o,%YDB-I-TEXT, glob filename pattern must end in ##TEST_SHL_SUFFIX##
		At M source location START+4^ydb974i

YDB>

# ydb974j.m starting. This tests setting zroutines to a glob pattern that does not end in .so.
# In this case, the filename ends in .m.
# We expect this to produce an error.
%YDB-E-ZROSYNTAX, $ZROUTINES syntax error: not*So##TEST_SHL_SUFFIX##.m,%YDB-I-TEXT, glob filename pattern must end in ##TEST_SHL_SUFFIX##
		At M source location START+4^ydb974j

YDB>

# Running ydb974k.m to test setting $zroutines to a glob pattern followed by parentheses.
# This should give us a NOLBRSRC error as it is not valid.
%YDB-E-ZROSYNTAX, $ZROUTINES syntax error: *974*.so(woops),%YDB-E-NOLBRSRC, Object libraries cannot have SRC paths associated
		At M source location START+3^ydb974k

YDB>

# Running ydb974l.m to test what happens when all elements in $zroutines
# are glob patterns with no matches.

# Running ydb974m.m to test what happens when the directory in a glob pattern does not have read permission.
# This should give us a FILEPARSE error and report Permission denied.
%YDB-E-ZROSYNTAX, $ZROUTINES syntax error: temp/fake*##TEST_SHL_SUFFIX##,%YDB-E-FILEPARSE, Error parsing file specification: temp/fake*##TEST_SHL_SUFFIX##,%SYSTEM-E-ENO13, Permission denied
		At M source location START+3^ydb974m

YDB>

# Starting ydb974o.m to check the error that happens
# when you try to auto-relink a shared object file
# We expect this to create an error as you cannot auto-relink a shared library object file.
%YDB-E-ZROSYNTAX, $ZROUTINES syntax error: ydb974la##TEST_SHL_SUFFIX##* $gtm_dist,%YDB-E-FILEPARSE, Error parsing file specification: ydb974la##TEST_SHL_SUFFIX##
		At M source location START+4^ydb974o

YDB>

# started ydb974p.m to test a wildcard 
# filename with an * in the middle of its name
# setting zroutines should not produce an error in this case.
# As the name matched an exact file, $zroutines the `*` should not be treated as a wildcard
# meaning that $zroutines should not have ydb974la##TEST_SHL_SUFFIX##
ydb9*74la##TEST_SHL_SUFFIX## ##SOURCE_REALPATH##(##SOURCE_REALPATH##)

# started test q to test the error that happens
# when there are more than 4096 elements in $zroutines.
# We expect an error saying that you can not add more than 4096.
%YDB-E-ZROSYNTAX, $ZROUTINES syntax error: manyMatches/ydb974la*.so,%YDB-E-MAXARGCNT, Maximum number of arguments 4096 exceeded
		At M source location START+4^ydb974q

YDB>

# Running ydb974r.m to test adding files a bunch of times
# As this previously caused a memory issue
# We expect this to exit without any error.

# Running ydb974s.m to test SRC paths in no match glob pattern.
# This should give an error saying that you cannot add SRC paths to a shared object library.
%YDB-E-ZROSYNTAX, $ZROUTINES syntax error: def*no*match.so(woops),%YDB-E-NOLBRSRC, Object libraries cannot have SRC paths associated
		At M source location START+3^ydb974s

YDB>

# Running ydb974t.m to test using ? in glob pattern.
# We expect *974l?.so to match both 974la.so and 974lb.so.
# So we expect both library function to run.
# $zroutines set, first try ydb974la
activated shared library a
# Next try ydb974lb
activated shared library b

# Running ydb974u.m to test using [] in glob pattern.
# We expect *974l[ab].so to match both 974la.so and 974lb.so.
# So we expect both library function to run.
# $zroutines set, first try ydb974la
activated shared library a
# Next try ydb974lb
activated shared library b
# Next we will try to match a source path with a ? glob char
# We expect this to work and appear in $zroutines.
20Matches/ydb974la1.so

# Running ydb974v.m to test the syntax error *lib*.so)
# We expect a syntax error to be reported.
%YDB-E-ZROSYNTAX, $ZROUTINES syntax error: *lib*.so)
		At M source location START+3^ydb974v

YDB>

# Running ydb974w.m to test the syntax error of setting zroutines
# to the glob pattern *o. This is an error as all glob patterns must end
# in .so. Thus, we expect an error saying that.
%YDB-E-ZROSYNTAX, $ZROUTINES syntax error: *o,%YDB-I-TEXT, glob filename pattern must end in .so
		At M source location START+4^ydb974w

YDB>

# Running ydb974x.m to test including glob characters
# in a filepath, we expect the glob characters to work and the library to be run
activated shared library a
# Now we test that the value of $zroutines is displayed correctly
# We expect $dirPathEnv/ydb974la1.so to be a direct path in output
# as it contains an environment variable.
20Matches/ydb974la1.so ##TEST_PATH##/manyMatches/ydb974la1.so ##SOURCE_REALPATH##(##SOURCE_REALPATH##)

# Starting test ydb974y.m for parse_file error with env var.
# We expect an error.
%YDB-E-ZROSYNTAX, $ZROUTINES syntax error: $fileparseErr,%YDB-E-FILEPARSE, Error parsing file specification: $fileparseErr,%YDB-E-FILENOTFND, File  not found
		At M source location START+3^ydb974y

YDB>

# Restore read permission for test environment.

# Creating a C shared library
# C shared library has been created, now it is time to try adding the
# C shared library to $zroutines and make sure that the other shared libraries run without issue.
# ZWriting $zroutines to make sure that it picked up the C library.
##TEST_AWK\$ZROUTINES=".*ydb974Ccode.so.*"
# Running routine from library a to make sure that no error occurs
activated shared library a
# Running routine from library b to make sure that no error occurs
activated shared library b

# Now test that an error within $ZROUTINES followed by an error outside $ZROUTINES.
# This test is motivated by the fact that it used to try to free memory twice in the above case.
# as described https://gitlab.com/YottaDB/DB/YDB/-/merge_requests/1817#note_3098461826
# A failed test would give an assert failure in sr_unix/zro_load.c or sr_port/gtm_malloc_src.h
# This test will now be expected to just give the 2 different errors.
# First a 'glob pattern must end in .so' error, then 'Either a space or an end-of-line was expected' error

YDB>
%YDB-E-ZROSYNTAX, $ZROUTINES syntax error: *.m
%YDB-I-TEXT, glob filename pattern must end in ##TEST_SHL_SUFFIX##

YDB>
%YDB-E-SPOREOL, Either a space or an end-of-line was expected but not found
	write "noEnd
	            ^-----

YDB>
