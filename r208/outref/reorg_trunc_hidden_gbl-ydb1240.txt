#------------------------------------------------------------------------------------------------#
# [#1240] MUPIP REORG -TRUNCATE should process globals in .dat files that are hidden by the .gld #
#------------------------------------------------------------------------------------------------#

Files Created in ##TEST_PATH##:
Using: ##SOURCE_PATH##/mumps -run GDE
mumps.gld
Using: ##SOURCE_PATH##/mupip
mumps.dat
# Run [inita^ydb1240] : grow ^x, create ^a at the very end of the file, then kill both
# Create 2reg.gld : a copy of the original gld plus the names a, b and c mapped to a new region AREG.
# Region DEFAULT's database file still holds those globals' blocks (right now the killed ^a's leftover
# root block, at the very end of the file) but 2reg.gld no longer maps the names to DEFAULT: they are
# hidden globals (see YDB#1240)

# Stage A : the YDB#1240 issue scenario (a killed global hidden by the gld)
#
# First run [mupip reorg -truncate -select=x -reg DEFAULT] : with an explicit -SELECT the reorg
# deliberately does NOT process hidden globals (the user decides the exact set of globals to work on).
# ^a's leftover root block therefore stays at the end of the file and pins it, since a single busy
# block in the last local bitmap rules out any meaningful truncation (usually a MUTRUNCALREADY message).
# The truncate tail sweep cannot help here either: a killed global's root block is empty, so the
# sweep's scan has no key to determine a global name from (only the directory tree still knows the
# name, and with -SELECT the reorg deliberately does not consult it beyond the selected names).
Hidden global ^a processed by the -select reorg : 0
Hidden ^a still pins the file after the -select reorg : YES

# Now run [mupip reorg -truncate -reg DEFAULT] : without -SELECT the reorg processes every global in
# region DEFAULT's directory tree, hidden ones included (the YDB#1240 fix), so ^a's root block moves
# to the front and the truncate works. A build without the fix reports this instead:
#	%YDB-I-MUTRUNCALREADY, Region DEFAULT: no further truncation possible
Hidden global ^a processed by the no-select reorg : 1
Truncated region: DEFAULT. Reduced total blocks from [MORE THAN 1024] to [1024 OR LESS]. Reduced free blocks from [ARBITRARY] to [LESS THAN 1024].

# Stage B : a hidden global WITH data
#
# Run [initb^ydb1240] (through the original gld) : regrow ^x, create ^b at the end of the file, then
# kill only ^x. With 2reg.gld the name [b] maps to AREG, so ^b (data and all) is a hidden global in
# region DEFAULT
# Run [mupip reorg -truncate -reg DEFAULT] : the hidden ^b's blocks must move down for the truncate
Hidden global ^b processed by the no-select reorg : 1
Truncated region: DEFAULT. Reduced total blocks from [MORE THAN 1024] to [1024 OR LESS]. Reduced free blocks from [ARBITRARY] to [LESS THAN 1024].
# Run [verifyb^ydb1240] (through the original gld) : ^b's contents must have survived the block moves
Verified contents of ^b : OK

# Stage C : hidden-global blocks that only the truncate tail sweep can move
#
# Run [initc^ydb1240] (through the original gld) : regrow ^x, create ^c at the end of the file, then
# kill only ^x (same shape as stage B, with ^c hidden by 2reg.gld)
# Run [mupip reorg -truncate -select=x -reg DEFAULT] : the explicit -SELECT keeps the reorg phase off
# the hidden ^c, but ^c's blocks hold live data (unlike stage A's killed ^a) and sit past the truncate
# point, so the tail sweep's scan finds them and determines the name [c] from their keys. Since that
# name is in region DEFAULT's own directory tree the sweep moves the blocks, no matter that the gld
# maps the name to AREG (the tail sweep part of the YDB#1240 fix; it used to skip such names)
Hidden global ^c processed by the reorg phase : 0
Tail sweep moved the hidden ^c : YES
Truncated region: DEFAULT. Reduced total blocks from [MORE THAN 1024] to [1024 OR LESS]. Reduced free blocks from [ARBITRARY] to [LESS THAN 1024].
# Run [verifyc^ydb1240] (through the original gld) : ^c's contents must have survived the sweep's moves
Verified contents of ^c : OK

# Stage D : a region ALL of whose globals are hidden
#
# Create 3reg.gld : a copy of 2reg.gld plus the name x also mapped to AREG; every name in region
# DEFAULT's directory tree (a, b, c and x) is now hidden
# Run [initx^ydb1240] (through the original gld) : regrow and kill ^x so a truncate has work to do
# Run [mupip reorg -truncate -reg DEFAULT] : a build without the YDB#1240 fix selects nothing here
# (every name is hidden) and errors out with a NOSELECT message; expect all four hidden globals
# processed and the file truncated instead
NOSELECT errors : 0
Hidden globals processed : 4
Truncated region: DEFAULT. Reduced total blocks from [MORE THAN 1024] to [1024 OR LESS]. Reduced free blocks from [ARBITRARY] to [LESS THAN 1024].

# Stage E : -EXCLUDE and hidden globals
#
# The -EXCLUDE list is built through the gld too ([gv_select]): a name that does not exist anywhere
# per the gld produces an EMPTY exclude list (a NOEXCLUDE message), so excluding a hidden global by
# name only works if the name also exists somewhere per the gld. Once the exclude list is non-empty,
# the exclusion check itself is name-based and applies to hidden globals like any other. Both
# behaviors are pre-existing -EXCLUDE semantics; this stage pins them down for hidden globals.

# Run [mupip reorg -truncate -exclude=b -reg DEFAULT] : the gld (3reg.gld) maps [b] to region AREG
# where no ^b exists, so expect a NOEXCLUDE message and the hidden ^b processed anyway
NOEXCLUDE messages : 1
Hidden global ^b processed despite -exclude=b : 1
EXCLUDEREORG messages : 0

# Run [initbareg^ydb1240] (through 3reg.gld) : create ^b in region AREG, so the name [b] now exists
# per the gld and -EXCLUDE=b builds a non-empty exclude list
# Rerun [mupip reorg -truncate -exclude=b -reg DEFAULT] : the exclusion check is name-based, so the
# hidden ^b in region DEFAULT is now excluded (an EXCLUDEREORG message) and the reorg ends with a
# REORGINC warning, the standard outcome whenever a selected global is excluded
----------
Error EXCLUDEREORG seen in truncate_exclude_visible.out as expected:
%YDB-W-EXCLUDEREORG, Global: b is present in the EXCLUDE option.  REORG will skip the global.
----------
----------
Error REORGINC seen in truncate_exclude_visible.out as expected:
%YDB-W-REORGINC, Reorg was incomplete.  Not all globals were reorged.
----------

# Stage F : a plain reorg (no -TRUNCATE) processes hidden globals too
#
# Run [mupip reorg -reg DEFAULT] : all four names in region DEFAULT's directory tree are hidden by
# 3reg.gld, so a build that invokes the hidden global selection only for -TRUNCATE selects nothing
# here (a NOSELECT error); expect all four processed instead, exactly as with -TRUNCATE in stage D
NOSELECT errors : 0
Hidden globals processed : 4
# Run [verifyb^ydb1240] and [verifyc^ydb1240] (through the original gld) : ^b and ^c must have
# survived the plain reorg intact
Verified contents of ^b : OK
Verified contents of ^c : OK

# Run [mupip reorg] (no -REGION at all, still through 3reg.gld) : the gld-visible ^b in region AREG
# and the hidden ^b in region DEFAULT are different globals with the same name; expect BOTH processed,
# each in its own region
^b processed in region AREG (gld-visible) : 1
^b processed in region DEFAULT (hidden) : 1

# Stage G : region types that mu_reorg_hidden_gbl_select must skip or handle specially
#
# mu_reorg_hidden_gbl_select walks EVERY region of the gld (when -REGION is not specified). Besides
# ordinary BG regions it has to handle four special cases without mishap: a statsDB region (skip), a
# non-BG/MM (dba_cm, i.e. remote GT.CM) region (skip -- it must NOT try to reach the remote node), a
# never-opened AUTODB region (skip WITHOUT auto-creating its database file), and a region that
# gv_select never opened -- e.g. one mapped only for LOCKs, which the global-name walk does not touch
# -- which it opens itself with gv_init_reg. Each case below uses its own gld and a plain (no -REGION,
# no -SELECT) MUPIP REORG, which is enough since mu_reorg_hidden_gbl_select runs for any REORG without
# -SELECT. The special regions carry no globals (only DEFAULT holds ^gtypes so the reorg has work and
# does not issue NOSELECT); the point is that the reorg walks the special region and comes through
# cleanly. The reorg output is examined only on failure, so the reference stays stable.
# G1 : a region mapped ONLY for LOCKs, plus statistics enabled on DEFAULT
#      -> gv_select's global-name walk never opens the lock-only region, so mu_reorg_hidden_gbl_select
#         opens it with gv_init_reg; DEFAULT's statsDB region (if instantiated) is walked and skipped
Plain reorg walked a lock-only region (line 113) and completed cleanly : YES
# G2 : a dba_cm (remote GT.CM) region, mapped only for LOCKs so gv_select never touches it
#      -> mu_reorg_hidden_gbl_select must skip it at the non-BG/MM check WITHOUT trying to reach the
#         remote host (there is no such host); the reorg completing cleanly proves the skip
Plain reorg skipped a dba_cm region (line 109) without remote access and completed cleanly : YES
# G3 : a never-opened AUTODB region, mapped only for LOCKs so gv_select never touches it
#      -> mu_reorg_hidden_gbl_select must skip it at the AUTODB check WITHOUT auto-creating its
#         database file; the file staying absent proves the skip
Plain reorg skipped an unopened AUTODB region (line 111) without creating its file : YES
##SOURCE_PATH##/mupip
##SOURCE_PATH##/mupip integ -REG *
No errors detected by integ.
